apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: blue
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/ucheor/Kustomize_ArgoCD_K8s_Deployment_Terraform_QR.git'
    path: kustomize-ArgoCD/Staging-Cluster/overlays/blue
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: staging-blue
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true