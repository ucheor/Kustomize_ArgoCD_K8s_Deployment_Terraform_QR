apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: green
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/ucheor/Kustomize_ArgoCD_K8s_Deployment_Terraform_QR.git'
    path: kustomize-ArgoCD/Staging-Cluster/overlays/green
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: staging-green
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true