apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: orange
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/ucheor/Kustomize_ArgoCD_K8s_Deployment_Terraform_QR.git'
    path: manifests-ArgoCD/Dev-Cluster/overlays/orange
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: orange
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true