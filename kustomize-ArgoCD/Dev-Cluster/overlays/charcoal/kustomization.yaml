apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/charcoal

# Adds a prefix or namespace specific to Cluster-A
namespace: dev-charcoal
namePrefix: dev-

# Built-in image tag management
images:
  - name: argocd-trf-k8s-demo-charcoal
    newName: ucheor/argocd-trf-k8s-demo-charcoal
    newTag: v1

replicas:
  - name: charcoal  # Make sure this matches the metadata.name of the deployment in your base folder
    count: 1

patches:
  - target:
      kind: Service
      name: dev-charcoal-svc
    patch: |-
      - op: replace
        path: /spec/ports/0/port
        value: 8082
      - op: replace
        path: /spec/type
        value: LoadBalancer