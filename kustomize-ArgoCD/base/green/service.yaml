apiVersion: v1
kind: Service
metadata:
  name: green-service
spec:
  selector:
    app: green
  ports:
    - protocol: TCP
      port: 80        # Port exposed by the service
      targetPort: 80   # Port the container is listening on
  type: ClusterIP      # Default internal-only type